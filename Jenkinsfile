@Library('SharedLibraries') _

pipeline {
    agent none
    stages {
        stage ('Test') {
            steps {
                script {
                    simple.call 'Kai Nguyen'
                }
            }
        }
    }
}